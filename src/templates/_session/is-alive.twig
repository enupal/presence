{% do view.registerAssetBundle("enupal\\presence\\web\\PresenceAsset") %}

{% js %}
    $(document).ready(function() {
        new EnupalPresence({{currentUser.id}}, {{ entryId }}, "{{ craft.presence.displayContainer }}");
    });
{% endjs %}